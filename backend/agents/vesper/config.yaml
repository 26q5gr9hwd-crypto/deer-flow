# THIS FILE IS NOT ACTIVE (dead/untracked copy)
# The active config.yaml loaded by paths.py is:
#   /opt/deer-flow/backend/.deer-flow/agents/vesper/config.yaml
# Edit that file, not this one.
# Note: backend/agents/ is an untracked directory not read by agents_config.py.
# Last updated: 2026-03-21 — marked as dead copy by VESPER-FIX-3 cleanup.
